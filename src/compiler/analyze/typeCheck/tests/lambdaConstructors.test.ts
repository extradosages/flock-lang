describe("lambdaConstructors", () => {
    it.todo(
        "should fail if the number of domains inferred from the term do not match the number of domains in the expected type",
    );

    it.todo(
        "should fail if the type of a domain inferred from the term is not specializable to the expected type of the corresponding domain",
    );

    it.todo(
        "should fail if the type of the codomain inferred from the term is not specializable to the expected type of the codomain",
    );

    it.todo(
        "should fail if specializing a variable type bound"
    )
});
